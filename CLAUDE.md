@AGENTS.md

## Claude Code Identity

Claude is the implementation agent for `sprintctl`. Primary language is Go. Treat the issue brief as the authoritative spec. For questions about shared tags, closeout obligations, or inter-agent protocol, answer from AGENTS.md only unless the user explicitly asks for Claude-specific behavior.
